#!/bin/bash
ANYCABLE_REDIS_URL=$REDIS_URL
REDIS=$REDIS_URL ADDR="0.0.0.0:$PORT"

if [ "$ANYCABLE_DEPLOYMENT" == "true" ]; then
  bundle exec anycable --server-command="anycable-go"
else
  bundle exec rails server -p $PORT -b 0.0.0.0
fi
